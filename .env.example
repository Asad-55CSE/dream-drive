VITE_API_URL=http://localhost:5000/api
# Must point to the server (better-auth base)
VITE_AUTH_URL=http://localhost:5000
# Must be the client's own origin — used to build absolute callbackURLs for OAuth
VITE_CLIENT_URL=http://localhost:5173
